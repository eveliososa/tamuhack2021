import * as React from "react";
import Box from "@material-ui/core/Box";
import Typography from "@material-ui/core/Typography";

export const OrganizationPage = () => {
    return(
        <Box>
            <Typography>Hello, Org!</Typography>
        </Box>
    );
};
